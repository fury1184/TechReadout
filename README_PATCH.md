# TechReadOut 3.5.6 Bug Fix

Fixes the AI JSON Import page and Open WebUI integration so both process exactly one hardware item and require one JSON object.

Unknown/unverified values must be returned as `null`; arrays are rejected.

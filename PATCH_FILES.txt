TechReadOut 3.5.9 Patch
========================

Apply over TechReadOut 3.5.8.

Changed files:
- app/version.py
- app/routes/api.py
- app/scrapers/scoring.py
- app/scrapers/validation.py
- tests/test_cpu_identity_matching.py

Changes:
- Requires exact CPU family/model/suffix/revision identity when recognizable.
- Rejects nearby Xeon candidates such as E5-2640 v4 for an E5-2696 v4 query.
- Prevents fuzzy word overlap and confidence scoring from overriding CPU mismatches.
- Sends unresolved/OEM CPU searches to AI/manual review instead of substituting a different CPU.
- Adds regression tests for Xeon and Intel Core naming variations.

No database migration is required.

After overlaying the files:
  cd /opt/stacks/techreadout
  docker compose up -d --build --force-recreate

Optional verification:
  docker compose exec app pytest -q tests/test_cpu_identity_matching.py
